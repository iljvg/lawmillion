# LawMillion — Environment Variables (sample)
# Copy to .env and fill in your real values. NEVER commit .env to git.

APP_ENV=production

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=lawmillion
DB_USER=lawmillion_app
DB_PASSWORD=change-me-please
